# Client connection configuration.
# Copy this file to client.env (next to the client executable) and adjust as needed.
# If client.env is absent the client defaults to localhost:5001.

SERVER_HOST=localhost
SERVER_PORT=5001
